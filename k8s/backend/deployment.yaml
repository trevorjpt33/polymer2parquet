apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: polymer2parquet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: REGION-docker.pkg.dev/PROJECT_ID/polymer2parquet/polymer2parquet-backend:latest
          envFrom:
            - configMapRef:
                name: polymer2parquet-config
            - secretRef:
                name: polymer2parquet-secrets
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /api/players/
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/players/
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"